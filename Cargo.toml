[package]
name = "ezrama"
version = "0.1.0"
edition = "2021"
description = "Keeps a TRYX Panorama SE display on its stored media without the vendor application running"
license = "MIT"

[dependencies]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
